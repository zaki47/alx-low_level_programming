#include "main.h"
/**
 * _abs - function computes the absolute value of an integer.
 * @n: int number
 * Return: 1 or 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
